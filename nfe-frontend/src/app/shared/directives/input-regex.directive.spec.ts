import { InputRegexDirective } from "./input-regex.directive";


describe('InputAlphaDirective', () => {
  it('should create an instance', () => {
    const directive = new InputRegexDirective();
    expect(directive).toBeTruthy();
  });
});
